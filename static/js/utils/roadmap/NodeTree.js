import { Node } from "./Node.js"

export class NodeTree {

    constructor({
        root = new Node({name: 'root'}),
    } = {}) {
        this.root = root;
    }

    insertNodeBefore(node) {
        
    }
}